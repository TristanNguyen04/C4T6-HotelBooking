export declare const parseDate: (dateStr?: string) => Date | null;
export declare const convertDateFormat: (dateStr?: string) => string;
export declare const calculateNights: (checkin: string, checkout: string) => number;
export declare const generateDefaultSearchUrl: (term: string, destination_id: string) => string;
