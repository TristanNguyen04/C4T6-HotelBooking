interface Step1FormProps {
    email: string;
    onEmailChange: (value: string) => void;
}
export default function Step1Form({ email, onEmailChange }: Step1FormProps): import("react/jsx-runtime").JSX.Element;
export {};
